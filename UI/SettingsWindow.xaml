<Window x:Class="PlanUp.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PlanUp Settings"
        Width="380"
        Height="460"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FAFAFA">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <TextBlock Grid.Row="0"
                   Text="Project Location"
                   FontSize="15"
                   FontWeight="SemiBold"
                   Foreground="#2C3E50"
                   Margin="0,0,0,12"/>

        <!-- COMUNA AND ZONE DROPDOWNS -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Comuna"
                       FontSize="12"
                       Foreground="#7F8C8D"
                       Margin="0,0,0,4"/>
            <ComboBox x:Name="ComunaCombo"
                      FontSize="13"
                      Padding="8,6"
                      SelectionChanged="ComunaCombo_SelectionChanged"/>

            <TextBlock Text="Zone"
                       FontSize="12"
                       Foreground="#7F8C8D"
                       Margin="0,12,0,4"/>
            <ComboBox x:Name="ZoneCombo"
                      FontSize="13"
                      Padding="8,6"
                      SelectionChanged="ZoneCombo_SelectionChanged"/>
        </StackPanel>

        <!-- PARAMETERS SECTION -->
        <TextBlock Grid.Row="2"
                   Text="PRC Parameters"
                   FontSize="15"
                   FontWeight="SemiBold"
                   Foreground="#2C3E50"
                   Margin="0,0,0,12"/>

        <Grid Grid.Row="3" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Max Height -->
            <TextBlock Grid.Row="0" Grid.Column="0"
                       Text="Max building height"
                       FontSize="12" Foreground="#34495E"
                       VerticalAlignment="Center" Margin="0,0,0,8"/>
            <StackPanel Grid.Row="0" Grid.Column="1"
                        Orientation="Horizontal" HorizontalAlignment="Right"
                        Margin="0,0,0,8">
                <TextBlock x:Name="HeightValue"
                           Text="--"
                           FontSize="13" FontWeight="SemiBold"
                           Foreground="#2C3E50" VerticalAlignment="Center"/>
                <TextBlock Text=" m" FontSize="12"
                           Foreground="#7F8C8D" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Setback Con Vano -->
            <TextBlock Grid.Row="1" Grid.Column="0"
                       Text="Setback (con vano)"
                       FontSize="12" Foreground="#34495E"
                       VerticalAlignment="Center" Margin="0,0,0,8"/>
            <StackPanel Grid.Row="1" Grid.Column="1"
                        Orientation="Horizontal" HorizontalAlignment="Right"
                        Margin="0,0,0,8">
                <TextBlock x:Name="ConVanoValue"
                           Text="--"
                           FontSize="13" FontWeight="SemiBold"
                           Foreground="#2C3E50" VerticalAlignment="Center"/>
                <TextBlock Text=" m" FontSize="12"
                           Foreground="#7F8C8D" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Setback Sin Vano -->
            <TextBlock Grid.Row="2" Grid.Column="0"
                       Text="Setback (sin vano)"
                       FontSize="12" Foreground="#34495E"
                       VerticalAlignment="Center" Margin="0,0,0,8"/>
            <StackPanel Grid.Row="2" Grid.Column="1"
                        Orientation="Horizontal" HorizontalAlignment="Right"
                        Margin="0,0,0,8">
                <TextBlock x:Name="SinVanoValue"
                           Text="--"
                           FontSize="13" FontWeight="SemiBold"
                           Foreground="#2C3E50" VerticalAlignment="Center"/>
                <TextBlock Text=" m" FontSize="12"
                           Foreground="#7F8C8D" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Rasante Angle -->
            <TextBlock Grid.Row="3" Grid.Column="0"
                       Text="Rasante angle"
                       FontSize="12" Foreground="#34495E"
                       VerticalAlignment="Center" Margin="0,0,0,8"/>
            <StackPanel Grid.Row="3" Grid.Column="1"
                        Orientation="Horizontal" HorizontalAlignment="Right"
                        Margin="0,0,0,8">
                <TextBlock x:Name="RasanteValue"
                           Text="--"
                           FontSize="13" FontWeight="SemiBold"
                           Foreground="#2C3E50" VerticalAlignment="Center"/>
                <TextBlock Text="°" FontSize="12"
                           Foreground="#7F8C8D" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- STATUS MESSAGE -->
        <TextBlock Grid.Row="4"
                   x:Name="StatusText"
                   Text=""
                   FontSize="11"
                   Foreground="#27AE60"
                   VerticalAlignment="Bottom"
                   Margin="0,0,0,12"/>

        <!-- BUTTONS -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="ApplyButton"
                    Content="Apply"
                    Width="80" Height="30"
                    FontSize="12"
                    Margin="0,0,8,0"
                    Click="ApplyButton_Click"
                    IsEnabled="False"/>
            <Button Content="Close"
                    Width="80" Height="30"
                    FontSize="12"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
